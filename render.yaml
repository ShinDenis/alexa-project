services:
  - type: web
    name: alexa-review
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir app app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.5